import { RolesDecoratorTsGuard } from './roles.decorator.ts.guard';

describe('RolesDecoratorTsGuard', () => {
  it('should be defined', () => {
    expect(new RolesDecoratorTsGuard()).toBeDefined();
  });
});
